class User {
  constructor(socket, id, address, token) {
    this.socket = socket;
    this.id = id;
    this.address = address;
    this.token = token;
  }

  // 클릭 체크하는 인터벌 만들기
}

export default User;
